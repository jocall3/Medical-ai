export class TumorGrowthSimulator { simulateGompertzian(initialVolume: number, growthRate: number, time: number): number { return initialVolume * Math.exp(Math.log(1000) * (1 - Math.exp(-growthRate * time))); } simulateLogistic(initialVolume: number, carryingCapacity: number, growthRate: number, time: number): number { return carryingCapacity / (1 + ((carryingCapacity - initialVolume) / initialVolume) * Math.exp(-growthRate * time)); } }